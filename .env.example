# G DESIGN environment configuration (copy to .env and fill real values)
# NOTE: values here are placeholders only — no backend code exists yet.

APP_ENV=local
APP_URL=http://localhost:8000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=gdesign
DB_USERNAME=root
DB_PASSWORD=

MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=hello@gdesign.co.tz
